var triangle = function() {
  var size = Number(prompt("Enter the size of the triangle", ''))
  if (size) {
    for (var i = #; i.length <= size; i += '#') {
      console.log(i);
    }
  } else {
    alert("You did not entered proper number..so exiting!");
  }
}


triangle();
